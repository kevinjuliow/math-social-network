# Math Social Network

## 1.Clone the repository
   ```bash
   git clone <repository-url>
   cd math-social-network
  ```
## 2. Server
   ```
   cd server
   npm install
   npx prisma generate
   ```

## 3. Client
  ```
  cd client
  npm install
  ```
